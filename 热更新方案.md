# PluginSdk 插件化热更新方案（SDK AAR + 空壳 Demo + 插件）

> 目标：对外提供 AAR SDK，接入方 App 只接入一次，后续通过「纯 dex + 资源」热更新
> 持续更新 SDK 能力与 UI。不使用第三方插件化框架，全程自研、简单可控。

---

## 一、方案结论

| 决策点 | 结论 |
|---|---|
| 对外产物 | **`sdk` 模块产出 AAR**（热更新引擎 + `PluginSdk` 对外 API） |
| 接入方（宿主） | 第三方 App，集成 AAR 后调用 `PluginSdk.init()`，有自己的 0x7f 资源 |
| 插件 | 纯源码目录 + `scripts/build_patch.py` 脚本构建 |
| 资源策略 | 插件资源用独立 `packageId 0x80`，与宿主 `0x7f` 隔离，互不冲突 |
| 签名校验 | 插件与宿主同签名（debug keystore），验签「补丁签名 == 宿主签名」 |
| 代码文件只读 | Android 14+ 强制要求，加载前 `setReadOnly()` |
| 热更粒度 | 类级别（新增/替换未加载类），重启 App 生效 |
| 运行环境 | minSdk 21，走 ART |
| 分发渠道 | 国内 ToB / 企业内部分发 |

---

## 二、架构（三层）

```
sdk (library → AAR)                     demo (application，模拟接入方)
┌──────────────────────────┐           ┌──────────────────────────────┐
│ PluginSdk（对外 API）      │           │ HostApplication              │
│  init / startPlugin / ... │           │ MainActivity（测试 UI）        │
│ hotupdate/                │ ──依赖──▶ │ AndroidManifest 预注册插件类名 │
│  DexLoader/ResourceLoader │           └──────────────────────────────┘
│  /SignatureVerifier ...   │
│ utils/AppLogUtils         │
└──────────────────────────┘

plugin（纯源码目录，build_patch.py 构建，资源 0x80）
┌──────────────────────────────┐
│ PluginEntry（入口）            │
│ PluginActivity（XML 布局）     │
│ PluginResources（资源自举）     │
│ res/（布局/图片/字符串/颜色）    │
└──────────────────────────────┘
```

- **`sdk`**：提供「宿主运行时」，把接入方 App 变成能加载插件的宿主；产出 AAR。
- **`demo`**：模拟一个接入方 App，`implementation project(':sdk')`，验证 SDK 接入流程。
- **`plugin`**：SDK 团队持续热更的补丁包，资源用 `0x80` 隔离。

---

## 三、目录结构

```
PluginSdk/
├── sdk/                                    # SDK（library，产出 AAR）
│   ├── build.gradle                        # namespace com.plugin.sdk
│   └── src/main/java/com/plugin/sdk/
│       ├── PluginSdk.java                  # 对外 API
│       ├── hotupdate/
│       │   ├── HotUpdateEngine.java        # 编排 找补丁→验签→dex→资源
│       │   ├── PatchRepository.java        # 补丁管理：定位/导入/删除（ensureWritable）
│       │   ├── PatchSignatureVerifier.java # 验签（补丁签名==宿主签名）
│       │   ├── DexLoader.java              # dex 合并 + setReadOnly
│       │   └── ResourceLoader.java         # 资源合并
│       └── utils/
│           └── AppLogUtils.java            # 日志工具（分段打印，tag 前缀 PluginSdk）
├── demo/                                   # 接入方 Demo（application）
│   ├── build.gradle                        # implementation project(':sdk')
│   └── src/main/
│       ├── AndroidManifest.xml             # 预注册插件 Activity 类名
│       ├── assets/patch.apk                # 补丁（快速验收用，可删除）
│       └── java/com/plugin/sdk/demo/
│           ├── HostApplication.java        # 调用 PluginSdk.init()
│           └── MainActivity.java           # 测试 UI
├── plugin/                                 # 插件（纯源码目录，脚本构建）
│   └── src/main/
│       ├── AndroidManifest.xml             # package=com.plugin.sdk.plugin
│       ├── java/com/plugin/sdk/plugin/
│       │   ├── PluginEntry.java            # 入口（宿主反射调用）
│       │   ├── PluginActivity.java         # 插件完整 Activity
│       │   └── PluginResources.java        # 插件资源自举
│       └── res/                            # 布局 / 图片 / 字符串 / 颜色
├── scripts/
│   └── build_patch.py                      # 一键构建插件（dex + 0x80 资源 + 签名）
└── 热更新方案.md                            # 本文档
```

---

## 四、对外 API（接入方「一次接入」）

接入方只需做两件事：

1. **Application 里初始化**：

```java
public class MyApp extends Application {
    @Override public void onCreate() {
        super.onCreate();
        PluginSdk.init(this);
    }
}
```

2. **AndroidManifest 预注册插件 Activity**（类名由 SDK 团队告知）：

```xml
<activity android:name="com.plugin.sdk.plugin.PluginActivity" android:exported="false"/>
```

其余 API：

| 方法 | 说明 |
|---|---|
| `PluginSdk.init(app)` | 初始化热更新引擎 |
| `PluginSdk.isPatchLoaded()` | 补丁是否已加载 |
| `PluginSdk.getPatchVersion()` | 补丁版本 |
| `PluginSdk.getPatchPath()` | 补丁路径 |
| `PluginSdk.getLastError()` | 上次加载失败原因 |
| `PluginSdk.importPatch(context)` | 从 /sdcard/Download 导入补丁 |
| `PluginSdk.startPluginActivity(context, className)` | 启动插件 Activity |

---

## 五、技术原理

### 1. dex 热更新（DexLoader）

`DexClassLoader` 加载补丁 dex → 反射合并 `dexElements` 到宿主 ClassLoader 最前面。
之后宿主进程内加载类都优先命中补丁类。只对「未加载过」的类可靠生效，「重启生效」规避边界。

### 2. 资源热更新（ResourceLoader + 插件自举）

- 宿主全局：新建 AssetManager 同时 `addAssetPath` 宿主 + 补丁，替换进程内 Resources 的 `mAssets`。
- 插件 Activity：`PluginResources` 自举，从 Intent 拿补丁路径，`addAssetPath` 加载补丁资源 inflate。

### 3. 资源 id 隔离（packageId 0x80）

插件资源用 `aapt2 link --package-id 0x80` 编译，与宿主 `0x7f` 错开，接入方有多少资源都不冲突。

```
Package name=com.plugin.sdk.plugin id=80
    resource 0x80010000 color/plugin_primary
    resource 0x80020000 drawable/plugin_bg
```

### 4. 签名校验（PatchSignatureVerifier）

插件与宿主同签名（debug keystore），验签比对「补丁签名证书 == 宿主签名证书」。
API 28+ 走 `SigningInfo`（V2/V3），API < 28 走 V1 签名。

### 5. 动态代码只读（Android 14+ 强制）

**Android 14（API 34）起，动态加载的 DEX/JAR/APK 文件必须 read-only**，否则抛：

```
SecurityException: Writable dex file '...' is not allowed.
```

原因：防止正在执行的 DEX 文件在执行过程中被修改（代码完整性）。因此 `DexLoader` 加载前
必须对补丁文件 `setReadOnly()`。

> 副作用处理：补丁一旦 `setReadOnly()`，后续覆盖（重新导入）会 `Permission denied`，
> 所以 `PatchRepository` 在写入前用 `ensureWritable()` 先恢复可写。

### 6. 日志（AppLogUtils）

`sdk` 内置 `AppLogUtils`（迁移自 BaseMyProject），tag 前缀 `PluginSdk`，超 4K 自动分段。
引擎全链路（找补丁 → 验签 → dex → 资源 → 版本）均已打日志，排查用：

```bash
adb logcat -s PluginSdk_HotUpdateEngine:V PluginSdk_SignatureVerifier:V
```

---

## 六、构建步骤

```bash
# 1. 构建插件（补丁 APK，资源 0x80，debug 签名 V1+V2）
python scripts/build_patch.py
# 产物：plugin/build/patch.apk

# 2. 构建 SDK（AAR）
./gradlew :sdk:assembleDebug
# 产物：sdk/build/outputs/aar/sdk-debug.aar

# 3. 构建 Demo（接入方 App）
./gradlew :demo:assembleDebug
# 产物：demo/build/outputs/apk/debug/demo-debug.apk
```

---

## 七、验收步骤

### 方式 A：快速验收（补丁已内置 assets）

1. `python scripts/build_patch.py`
2. `cp plugin/build/patch.apk demo/src/main/assets/patch.apk`
3. `./gradlew :demo:assembleDebug`
4. `adb install -r demo/build/outputs/apk/debug/demo-debug.apk`
5. 打开 App，状态显示「补丁已加载 / 版本 2.0.0」→ 点「加载补丁 View」「启动补丁 Activity」验证。

### 方式 B：真热更验收（运行时导入，重启生效）

1. 删除 `demo/src/main/assets/patch.apk`，重编译安装（首次显示「补丁未加载」）；
2. `adb push plugin/build/patch.apk /sdcard/Download/patch.apk`
3. App 内点「从 sdcard 导入补丁」，杀掉进程重开，补丁生效。

---

## 八、约束与注意事项

1. **补丁签名必须与宿主一致**：换 keystore 时，`build_patch.py` 与宿主签名同步改，否则验签失败。
2. **多插件**：需为每个插件分配不同 packageId（0x80、0x81…），在 `build_patch.py` 改 `PACKAGE_ID`。
3. **验签时机**：必须在 `DexClassLoader` 加载之前完成。
4. **代码文件必须 read-only**：Android 14+ 强制，`DexLoader` 已内置 `setReadOnly()`，勿移除。
5. **补丁分发**：生产环境将 `importFromSdcard` 替换为 HTTPS 下载 + 版本管理 + 灰度 + 回滚。

---

## 九、可信插件加载链（生产化要求）

骨架阶段补丁从 assets / sdcard 导入。生产环境应形成完整可信链，防止「App 安装后，服务器
仍可改变 App 可执行代码」带来的完整性风险。正确流程：

```
① 下载到临时文件
② 下载完成
③ SHA-256 校验（下载的东西是不是原来的东西）
④ 数字签名校验（是不是服务器发布的）     ← 骨架已有验签
⑤ rename 成正式文件
⑥ setReadOnly()                          ← 骨架已有
⑦ DexClassLoader
```

建议补丁目录带元数据：

```
plugins/
└── game/
    └── 100/
        ├── plugin.dex
        └── plugin.json        # { "version": 100, "sha256": "xxx", "signature": "xxx" }
```

对照骨架现状：

| 环节 | 状态 |
|---|---|
| 数字签名（验签） | ✅ 已有 |
| read-only | ✅ 已有 |
| HTTPS 下载 | ⚠️ 未做（骨架走 assets/sdcard） |
| SHA-256 显式校验 | ⚠️ 未做（签名已隐含完整性） |
| 版本管理 / 回滚 | ⚠️ 未做（`getVersion()` 只读不比对） |

---

## 十、后续扩展建议

1. **补丁分发**：HTTPS 下载 + 版本号比对 + SHA-256 + 灰度 + 回滚。
2. **多 dex**：插件变大后自动拆 `classes.dex`，`DexLoader` 天然兼容。
3. **native 库**：`DexClassLoader` 的 nativeLibraryDir 参数已预留。
4. **日志开关**：`AppLogUtils` 目前硬编码开启，发布前可接入 BuildConfig 动态开关。
