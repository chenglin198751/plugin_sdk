# PluginSdk 插件化热更新方案（当前实现）

> 当前状态：最小可运行方案已完成并在真机验证通过。插件 View 可以正常加载，插件代理 Activity 可以正常启动，插件 XML、字符串、颜色和 drawable 资源可以正常渲染。

## 一、方案结论

- 对外提供 `sdk` 模块生成的 AAR。
- 第三方 App 只接入一次 SDK，后续通过插件 APK 更新代码和 UI。
- 插件由 Android Gradle Plugin 正常编译，不使用 Python 或第三方插件化框架。
- 插件支持 dex、XML 布局、字符串、颜色和 drawable 资源。
- 更新后的补丁需要重启 App 生效。
- 当前只保证新增类和“尚未被加载类”的替换，不做已加载类的方法级替换。
- 目标场景：minSdk 21、ART、国内 ToB / 企业内部分发。

## 二、三层结构

```text
PluginSdk/
├── sdk/       Android library，对外输出 AAR
├── demo/      模拟第三方接入方的测试 App
└── plugin/    可热更插件，输出插件 APK
```

### `sdk`

SDK 运行时，负责：

- `PluginSdk` 对外 API；
- 补丁定位、导入和状态管理；
- APK 签名校验；
- dex 加载和类优先级处理；
- 插件独立 Resources / Theme；
- `HostProxyActivity` 宿主占位 Activity；
- 插件 Activity 生命周期代理；
- 日志和错误信息。

### `demo`

模拟真实接入方：

- 依赖 `sdk`；
- 在 `Application.onCreate()` 调用 `PluginSdk.init(this)`；
- 注册 `HostProxyActivity`；
- 界面来自宿主自己的 XML 布局 `res/layout/activity_main.xml`；
- 验证插件 View 和插件 Activity。

真实接入方不需要依赖 `plugin` 模块。

宿主拥有自己的 `0x7f` 资源，与插件资源 ID 完全撞车（例如双方都有
`0x7f020000`），但由于二者分属不同的 `Resources` 对象，各自仍能正确解析，
这也是资源隔离效果最直接的验证场景。

### `plugin`

独立 Android application 模块，由 AGP 编译为完整插件 APK，包含：

- `classes.dex`；
- `resources.arsc`；
- XML、drawable、颜色、字符串等资源；
- 插件入口和页面代码。

插件中的页面不是 `android.app.Activity`，而是普通 Java 类，由 SDK 的宿主占位 Activity 承载。

## 三、接入方接入方式

### 1. 初始化

```java
public class MyApplication extends Application {
    @Override
    public void onCreate() {
        super.onCreate();
        PluginSdk.init(this);
    }
}
```

### 2. 注册占位 Activity

接入方只需要注册 SDK 提供的占位组件：

```xml
<activity
    android:name="com.plugin.sdk.activity.HostProxyActivity"
    android:exported="false"
    android:theme="@android:style/Theme.Material.NoActionBar" />
```

不需要在 Manifest 注册插件中的 `PluginActivity`。

### 3. 对外 API

| API | 作用 |
|---|---|
| `PluginSdk.init(Application)` | 初始化热更新引擎 |
| `PluginSdk.isPatchLoaded()` | 查询补丁是否加载成功 |
| `PluginSdk.getPatchVersion()` | 获取补丁版本 |
| `PluginSdk.getPatchPath()` | 获取当前补丁路径 |
| `PluginSdk.getLastError()` | 获取最近一次失败原因 |
| `PluginSdk.importPatch(Context)` | 从 `/sdcard/Download/patch.apk` 导入补丁 |
| `PluginSdk.startPluginActivity(Context)` | 启动插件代理 Activity |

当前插件页面选择参数固定为：

```text
plugin_view_id = 1
```

后续可以扩展为页面注册表或多个页面 ID。

## 四、Edge-to-edge 全屏适配

SDK 内置 `com.plugin.sdk.utils.EdgeToEdge`，行为对齐 AndroidX 的
`androidx.activity.EdgeToEdge`，但用纯 framework API 实现，**不引入任何 AndroidX 依赖**，
保持 SDK 零依赖。

### 两种用法

**1. 接入方在自己的 Activity 上调用**

```java
@Override
protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    EdgeToEdge.enable(this);
    ...
}
```

**2. 插件页面自动开启**

`HostProxyActivity` 在 `onCreate` 中固定调用 `EdgeToEdge.enable(this)`，插件页面开箱即全屏，
不提供开关。

### 与 AndroidX 的 API 对应关系

| AndroidX | framework 等价 | 引入版本 |
|---|---|---|
| `WindowCompat.setDecorFitsSystemWindows` | `window.setDecorFitsSystemWindows(false)` | API 30 |
| 同上，低版本回退 | `decorView.setSystemUiVisibility(LAYOUT_STABLE\|LAYOUT_FULLSCREEN\|LAYOUT_HIDE_NAVIGATION)` | API 16 |
| `window.statusBarColor` | `window.setStatusBarColor(int)` | **API 21** |
| `window.navigationBarColor` | `window.setNavigationBarColor(int)` | **API 21** |
| `window.isStatusBarContrastEnforced` | `window.setStatusBarContrastEnforced(boolean)` | **API 29** |
| `window.isNavigationBarContrastEnforced` | `window.setNavigationBarContrastEnforced(boolean)` | **API 29** |
| `isAppearanceLightStatusBars` | `SYSTEM_UI_FLAG_LIGHT_STATUS_BAR` / API 30+ `getInsetsController()` | **API 23** |
| `isAppearanceLightNavigationBars` | `SYSTEM_UI_FLAG_LIGHT_NAVIGATION_BAR` / API 30+ 同上 | **API 26** |

`EdgeToEdge.enable()` 内部按 API 21 / 23 / 26 / 29 / 30 分支，覆盖 minSdk 21 起全部版本。
API 30 才存在的 `WindowInsetsController` 等类型集中在内部类 `EdgeToEdge$Api30` 中，
只在 API 30+ 被加载，低版本设备不会做该类校验。

### 注意事项

1. **API 35+ 蒙层颜色失效**：targetSdk 35 起系统强制 edge-to-edge，
   `setStatusBarColor` / `setNavigationBarColor` 变为 no-op，`SystemBarStyle` 传入的
   scrim 颜色在 Android 15+ 上不会生效。这是平台行为，官方 AndroidX 实现同样如此。
2. **插件页面需要处理 inset 避让**：开启全屏后内容会绘制到系统栏下方，
   插件布局需要自行留出状态栏/导航栏间距，否则内容可能被遮挡。
3. **接入方自定义样式**：可用 `EdgeToEdge.enable(activity, statusBarStyle, navigationBarStyle)`
   分别定制状态栏和导航栏，`SystemBarStyle` 提供 `auto` / `dark` / `light` 三种预设，
   并支持通过 `DarkModeDetector` 接入自己的夜间模式判断。

## 五、补丁启动流程

```text
Application.onCreate()
        ↓
PluginSdk.init()
        ↓
HotUpdateEngine.load()
        ├─ 查找 filesDir/patch/patch.apk
        ├─ 找不到时尝试导入 assets/patch.apk
        ├─ 校验插件签名
        ├─ DexLoader 加载 classes.dex
        ├─ 初始化插件 Resources
        ├─ 读取插件版本
        └─ 标记补丁加载成功
```

正式补丁路径：

```text
/data/user/0/<host-package>/files/patch/patch.apk
```

`assets/patch.apk` 仅用于 demo 快速验收。生产环境可替换为企业内部下载或文件导入流程。

## 六、dex 热更新

`DexLoader` 的核心流程：

1. 对补丁 APK 执行 `setReadOnly()`；
2. 使用 `DexClassLoader` 加载补丁；
3. 反射读取宿主和补丁的 `BaseDexClassLoader.pathList.dexElements`；
4. 将补丁 dexElements 放到宿主 dexElements 前面；
5. 写回宿主 ClassLoader。

```text
merged dexElements = patchElements + hostElements
```

因此后续类加载会优先命中补丁类。

### 生效边界

- 新增类可以加载；
- 尚未被 ART 加载的类可以由补丁优先提供；
- 已加载类不保证可以替换；
- 导入补丁后需要重启 App；
- 不涉及 ArtMethod 或 native 方法热替换。

### Android 14+ 只读要求

Android 14（API 34）开始，动态加载的 DEX/JAR/APK 必须只读，否则会出现：

```text
SecurityException: Writable dex file ... is not allowed
```

当前加载前显式执行：

```java
patchFile.setReadOnly();
```

补丁重新导入前，`PatchRepository` 会先恢复旧文件可写，避免覆盖失败。优化目录使用：

```text
app.getCodeCacheDir()/patch_dex
```

放入 `code_cache` 本身不等于只读，`setReadOnly()` 才是关键。

## 七、插件资源方案

### 1. 当前实际方案

插件使用 AGP 正常编译，资源默认使用 `0x7f`。当前不使用：

- `scripts/build_patch.py`；
- 手写 Python `aapt2` 链路；
- `--package-id 0x80`；
- `--shared-lib`；
- `R.onResourcesLoaded()`；
- `addAssetPathAsSharedLibrary()`。

插件与宿主都可能拥有 `0x7f` 资源 ID，但两者使用不同的 `AssetManager` 和 `Resources` 对象，因此不会直接在同一个资源表中冲突。

当前 demo 与插件就处于完全撞车的状态：

| 资源 ID | 宿主（demo） | 插件（plugin） |
|---|---|---|
| `0x7f010000` | `id/btn_import_patch` | `color/plugin_primary` |
| `0x7f020000` | `layout/activity_main` | `drawable/plugin_bg` |
| `0x7f030000` | `string/btn_import_patch` | `id/iv_plugin_logo` |

双方同为 `0x7f` 且 ID 逐一冲突，仍能各自解析到自己的资源。

### 2. 初始化资源

`PluginResources.init()` 会：

1. 创建独立 `AssetManager`；
2. 通过 `addAssetPath(patchApkPath)` 加载插件 APK；
3. 检查返回 cookie；
4. 创建只包含插件资源的 `Resources`；
5. 复用宿主的 DisplayMetrics 和 Configuration；
6. 创建插件 Theme，并复制宿主 Theme 的系统样式属性；
7. 验证插件布局、背景和图标资源。

### 3. 插件 Context 和 Theme

仅替换 `getResources()` 不够。XML inflate 创建 View 时，还会通过 `context.obtainStyledAttributes()` 使用 `context.getTheme()` 解析 `background`、`src`、`textColor` 等属性。

当前 `PluginContext` 同时提供：

```text
getResources() → 插件 Resources
getAssets()   → 插件 AssetManager
getTheme()    → 插件 Theme
```

inflate 时使用：

```java
LayoutInflater.from(host)
        .cloneInContext(pluginContext)
        .inflate(layoutId, null);
```

这解决了之前的错误：

```text
Unable to find resource ID #0x7f020000
```

当时资源确实存在于插件 APK，但 View 的属性解析仍使用宿主 Theme，导致宿主 Resources 无法找到插件的 `plugin_bg`。现在插件的 XML 属性解析也走插件 Resources，View 和 Activity 资源均已验证通过。

## 八、插件 Activity 代理

插件 Activity 采用宿主占位 Activity + 普通 Java 类的代理模式：

```text
PluginSdk.startPluginActivity()
        ↓
启动 sdk 的 HostProxyActivity
        ↓
ApkPluggingActivityProxy 反射加载插件 ApkProxyActivity
        ↓
ApkProxyActivity 根据 plugin_view_id 创建 PluginActivity
        ↓
PluginActivity 通过 PluginBaseActivity 加载插件 XML
        ↓
插件 View 设置到 HostProxyActivity
```

主要类职责：

- `HostProxyActivity`：宿主真正注册和启动的 Activity；
- `ApkPluggingActivityProxy`：宿主侧反射代理，转发生命周期；
- `ApkProxyActivity`：插件侧生命周期总代理；
- `PluginBaseActivity`：持有宿主 Activity 和插件根 View；
- `PluginActivity`：当前示例插件页面。

插件页面的生命周期接口通过约定的方法签名配合，不依赖宿主侧 Java 类型。

## 九、插件入口和 View 加载

宿主通过反射调用：

```text
com.plugin.sdk.plugin.PluginEntry
```

当前入口方法：

```java
public static String getVersion();
public static void initResources(Context context, String patchPath);
public static View createView(Context context);
```

直接加载 View 的流程：

```text
反射 PluginEntry.createView()
        ↓
PluginResources.inflate()
        ↓
加载插件 XML、drawable、color、string
        ↓
返回 View 并嵌入宿主页面
```

## 十、签名校验

当前安全模型：

```text
插件 APK 签名证书 == 宿主 APK 签名证书
```

实现策略：

- API 28+：`GET_SIGNING_CERTIFICATES` + `SigningInfo` + `getApkContentsSigners()`；
- API 27 及以下：`GET_SIGNATURES`，走 V1 证书比对；
- 日志输出补丁和宿主证书 SHA-256 指纹；
- 验签通过后才会进入 dex 加载阶段。

当前 demo/plugin release 使用同一台机器的 debug keystore：

```text
~/.android/debug.keystore
alias: androiddebugkey
password: android
```

生产环境必须替换为企业正式签名，并保证宿主和插件使用同一签名证书。

### 关于严格 V2-only

当前代码校验的是“宿主和插件证书一致”，并未自行解析 APK Signing Block 来强制禁止 V1/V3。因此当前实现不等同于严格的 V2-only。

如果生产要求仍然是“仅 V2”，后续还需要：

- 构建阶段明确关闭不需要的签名方案；
- 增加 APK Signing Block 的 V2 级别校验。

## 十一、自动化构建

### 推荐命令

从项目根目录执行：

```bash
./gradlew :demo:assembleDebug
```

Windows 也可以执行：

```bat
gradlew.bat :demo:assembleDebug
```

自动流程：

```text
:demo:assembleDebug
        ↓
自动执行 :plugin:assembleRelease
        ↓
复制 plugin-release.apk
        ↓
demo/src/main/assets/patch.apk
        ↓
继续编译 demo
```

因此不再需要手动执行插件编译和 `cp`。

### 单独构建插件

```bash
./gradlew :plugin:assembleRelease
```

产物：

```text
plugin/build/outputs/apk/release/plugin-release.apk
```

插件构建完成后也会自动复制为：

```text
demo/src/main/assets/patch.apk
```

### 构建 SDK AAR

```bash
./gradlew :sdk:assembleDebug
```

产物：

```text
sdk/build/outputs/aar/sdk-debug.aar
```

### 当前工具链

```text
Android Gradle Plugin 9.4.0
Gradle 9.7.1
compileSdk 37
 targetSdk 37
minSdk 21
Java 17
```

## 十二、验收

```bash
./gradlew :demo:assembleDebug
adb install -r demo/build/outputs/apk/debug/demo-debug.apk
```

启动 demo 后验证：

1. 状态显示补丁已加载，版本为 `2.0.0`；
2. 点击“加载补丁 View（dex+资源）”，插件布局和资源正常显示；
3. 点击“启动补丁 Activity”，插件代理 Activity 正常启动；
4. 返回、暂停、恢复等生命周期正常转发。

以上 View 和 Activity 均已在真机验证通过。

运行时导入补丁可以使用：

```bash
./gradlew :plugin:assembleRelease
adb push plugin/build/outputs/apk/release/plugin-release.apk /sdcard/Download/patch.apk
```

然后在 demo 中点击“从 sdcard 导入补丁”，重启 App 后生效。

## 十三、日志和排查

SDK 使用迁移后的 `AppLogUtils`，日志 tag 自动带 `PluginSdk_` 前缀，并支持超过 4K 自动分段：

```bash
adb logcat -s PluginSdk_HotUpdateEngine:V PluginSdk_SignatureVerifier:V PluginSdk_PluginSdk:V
```

插件资源和页面还会输出：

```text
PluginResources
PluginBaseActivity
```

关键日志：

```text
开始加载补丁
验签结果
 dex 加载成功
补丁资源初始化成功
addAssetPath 成功
插件资源验证
inflate layout
```

## 十四、当前限制和生产注意事项

1. 当前只保证新增类或未加载类的替换，补丁导入后必须重启。
2. `DexClassLoader.pathList.dexElements`、`DexPathList.dexElements` 和 `AssetManager.addAssetPath` 属于内部/隐藏实现，需要持续验证 Android 版本兼容性。
3. 当前插件只依赖 Android Framework API，没有加入 AndroidX 或其他三方库。若插件增加依赖，需要单独处理库打包、版本冲突和 ClassLoader 优先级。
4. 当前骨架支持 assets 和 `/sdcard/Download/patch.apk`，尚未实现 HTTPS 下载、版本比较、灰度、回滚和重试。
5. 生产下载流程应采用：

```text
下载到临时文件
→ SHA-256 校验
→ 签名校验
→ 版本校验
→ 原子 rename
→ setReadOnly()
→ 重启后加载
```

## 十五、当前已验证结论

- `sdk` 可以作为 AAR 对外提供；
- `demo` 可以自动触发插件编译；
- 宿主使用自己的 XML 布局和 `0x7f` 资源；
- 插件 APK 可以正常加载 dex；
- 宿主和插件签名校验通过；
- Android 14+ 动态 dex 只读问题已解决；
- 插件独立 XML Resources 和 Theme 已打通；
- 宿主与插件同为 `0x7f` 且 ID 冲突时仍可各自正确解析；
- 插件 View 可以正常加载；
- 插件代理 Activity 可以正常启动；
- 插件生命周期可以转发；
- 当前推荐命令为：

```bash
./gradlew :demo:assembleDebug
```
