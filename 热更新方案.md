# PluginSdk 插件化热更新整体实现方案

> 当前版本：最小可运行骨架已打通
>
> 当前验证结果：插件 View 可以正常加载，插件代理 Activity 可以正常启动，插件 XML 布局、字符串、颜色和本地图形资源均可以正常渲染。

---

## 一、项目目标

本项目实现一套不依赖第三方插件化框架的 Android SDK 热更新方案：

- 对外提供一个 `sdk` AAR；
- 第三方接入方只接入一次 SDK；
- 后续通过插件 APK 更新代码和 UI；
- 插件支持 Java 代码、XML 布局、字符串、颜色、drawable 等资源；
- 插件可以以 View 形式嵌入宿主页面；
- 插件可以以 Activity 代理形式独立启动；
- 插件更新后重启 App 生效；
- 代码层面只覆盖新增类，以及尚未被加载类的替换；
- 不使用第三方插件化框架，核心机制自研、可控、便于企业内部维护。

当前目标场景：

| 项目 | 当前方案 |
|---|---|
| 最低版本 | Android API 21 |
| 运行时 | 只考虑 ART |
| 分发场景 | 国内 ToB / 企业内部分发 |
| 宿主 | 第三方接入方 App |
| 对外交付物 | `sdk` 模块生成的 AAR |
| 插件交付物 | AGP 编译生成的插件 APK |
| 生效方式 | 导入补丁后重启 App |
| UI 资源 | 插件 APK 自带 XML 和资源 |
| 第三方框架 | 不使用 |

---

## 二、三层架构

```text
PluginSdk/
├── sdk/       SDK 运行时，Android library，输出 AAR
├── demo/      模拟第三方接入方的测试 App
└── plugin/    可热更新插件，Android application，输出插件 APK
```

### 1. `sdk` 模块：对外 AAR

`sdk` 不是最终宿主 App，而是提供宿主运行时能力的 Android library。

它负责：

- 对外暴露 `PluginSdk` API；
- 管理补丁文件；
- 验证补丁签名；
- 加载补丁 dex；
- 初始化插件独立资源；
- 提供宿主占位 `HostProxyActivity`；
- 将宿主 Activity 生命周期反射转发给插件页面；
- 输出日志和错误信息。

最终由第三方 App 通过 Gradle 依赖这个模块生成的 AAR。

### 2. `demo` 模块：第三方接入方示例

`demo` 是一个普通 Android application，用来模拟真实接入方 App：

- 依赖 `implementation project(':sdk')`；
- 在 `Application.onCreate()` 调用 `PluginSdk.init(this)`；
- 在 Manifest 注册 SDK 提供的 `HostProxyActivity`；
- 通过按钮验证插件 View 和插件 Activity；
- 运行 `:demo:assembleDebug` 时自动编译插件并同步到 assets。

真实接入方不需要依赖 `plugin` 模块，`plugin` 只属于 SDK 发布方的插件构建工程。

### 3. `plugin` 模块：热更新插件

`plugin` 是独立的 Android application 模块，由 Android Gradle Plugin 正常编译成 APK。

插件 APK 内包含：

- `classes.dex`：插件 Java 代码；
- `resources.arsc`：插件资源表；
- `res/`：XML 布局、drawable、颜色、字符串等资源；
- 插件自己的 Manifest 和签名信息。

插件不声明真正的 Android Activity。插件页面是普通 Java 类，由 SDK 侧的宿主占位 Activity 承载。

---

## 三、当前目录结构

```text
PluginSdk/
├── build.gradle
├── settings.gradle
├── gradle.properties
├── gradle/
│
├── sdk/                                      # 对外 SDK，输出 AAR
│   ├── build.gradle
│   └── src/main/java/com/plugin/sdk/
│       ├── PluginSdk.java                    # 对外 API
│       ├── activity/
│       │   ├── ApkInterfaceForProxyActivity.java
│       │   ├── ApkPluggingActivityProxy.java # 宿主侧生命周期代理
│       │   └── HostProxyActivity.java        # 宿主 Manifest 注册的占位 Activity
│       ├── hotupdate/
│       │   ├── HotUpdateEngine.java          # 找补丁、验签、加载 dex、加载资源
│       │   ├── PatchRepository.java          # 补丁定位、导入、删除、恢复可写
│       │   ├── PatchSignatureVerifier.java   # 补丁签名校验
│       │   ├── DexLoader.java                # DexClassLoader 和 dexElements 合并
│       │   └── ResourceLoader.java           # 早期资源加载代码，当前不是主路径
│       └── utils/
│           └── AppLogUtils.java              # 分段日志工具
│
├── demo/                                     # 模拟接入方 App
│   ├── build.gradle
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── assets/
│       │   └── patch.apk                     # 自动生成的快速验收补丁
│       └── java/com/plugin/sdk/demo/
│           ├── HostApplication.java          # 调用 PluginSdk.init()
│           └── MainActivity.java             # 验收 UI
│
├── plugin/                                   # 热更新插件 APK
│   ├── build.gradle
│   ├── src/main/AndroidManifest.xml
│   ├── src/main/java/com/plugin/sdk/plugin/
│   │   ├── ApkInterfaceForProxyActivity.java
│   │   ├── ApkProxyActivity.java             # 插件侧生命周期总代理
│   │   ├── PluginBaseActivity.java           # 普通插件页面基类
│   │   ├── PluginActivity.java               # 示例插件页面
│   │   ├── PluginEntry.java                  # 宿主反射调用的入口
│   │   └── PluginResources.java              # 插件独立 Resources 和 Theme
│   └── src/main/res/
│       ├── drawable/
│       │   ├── plugin_bg.xml
│       │   └── plugin_icon.xml
│       ├── layout/plugin_activity.xml
│       └── values/
│           ├── colors.xml
│           └── strings.xml
│
└── 热更新方案.md                             # 本文档
```

---

## 四、接入方使用方式

### 1. 依赖 SDK AAR

真实接入方只需要依赖 `sdk` 输出的 AAR。当前 demo 中使用工程依赖：

```groovy
dependencies {
    implementation project(':sdk')
}
```

发布时可以替换为本地 AAR、企业 Maven 仓库或其他内部制品仓库。

### 2. Application 初始化

接入方在自己的 `Application` 中初始化一次：

```java
public class MyApplication extends Application {

    @Override
    public void onCreate() {
        super.onCreate();
        PluginSdk.init(this);
    }
}
```

初始化流程会在进程启动时执行。补丁导入后需要重启 App，下一次启动时生效。

### 3. Manifest 注册宿主占位 Activity

真实接入方需要注册 SDK 提供的占位 Activity：

```xml
<activity
    android:name="com.plugin.sdk.activity.HostProxyActivity"
    android:exported="false"
    android:theme="@android:style/Theme.Material.NoActionBar" />
```

不需要注册插件中的 `PluginActivity`，因为插件中的 `PluginActivity` 不是 Android Framework Activity，而是普通 Java 页面类。

### 4. 对外 API

| API | 说明 |
|---|---|
| `PluginSdk.init(Application app)` | 初始化热更新引擎，通常在 Application 中调用一次 |
| `PluginSdk.isPatchLoaded()` | 返回补丁是否加载成功 |
| `PluginSdk.getPatchVersion()` | 返回插件版本 |
| `PluginSdk.getPatchPath()` | 返回当前补丁路径 |
| `PluginSdk.getLastError()` | 返回上一次加载失败原因 |
| `PluginSdk.importPatch(Context context)` | 从 `/sdcard/Download/patch.apk` 导入补丁 |
| `PluginSdk.startPluginActivity(Context context)` | 启动插件代理 Activity |

当前 `startPluginActivity()` 内部默认传递：

```text
plugin_view_id = 1
```

后续可以扩展为多个插件页面，通过不同的 view ID 选择插件页面实现。

---

## 五、补丁启动和加载总流程

```text
Application.onCreate()
        │
        ▼
PluginSdk.init(this)
        │
        ▼
HotUpdateEngine.load()
        │
        ├─ 1. 查找 filesDir/patch/patch.apk
        │       │
        │       └─ 找不到时尝试从 assets/patch.apk 导入
        │
        ├─ 2. 校验插件 APK 签名
        │
        ├─ 3. DexLoader 加载 classes.dex
        │       ├─ setReadOnly()
        │       ├─ DexClassLoader
        │       └─ 合并 dexElements
        │
        ├─ 4. 反射调用 PluginEntry.initResources()
        │
        ├─ 5. 反射读取 PluginEntry.getVersion()
        │
        └─ 6. 标记补丁加载成功
```

补丁正式路径为：

```text
/data/user/0/<host-package>/files/patch/patch.apk
```

`assets/patch.apk` 只是 demo 快速验收来源。真实热更新可以由网络下载、文件导入或企业内部更新服务提供。

---

## 六、dex 热更新实现

### 1. `DexLoader` 工作原理

`DexLoader` 使用：

```java
DexClassLoader patchLoader = new DexClassLoader(
        patchApkPath,
        optimizedDir,
        null,
        hostLoader);
```

然后通过反射取得：

```text
BaseDexClassLoader.pathList.dexElements
```

将插件 dexElements 放在宿主 dexElements 前面：

```text
merged = patchElements + hostElements
```

写回宿主 ClassLoader 后，后续通过 `Class.forName()` 或宿主代码加载类时，会优先命中插件 dex 中的类。

### 2. 生效边界

当前方案是类级别热更新：

- 新增类：可以加载；
- 尚未加载的类：可以由插件版本优先提供；
- 已经被 ART 加载并执行的类：不能保证运行中被替换；
- 推荐补丁导入后重启 App；
- 不涉及 ArtMethod 修改、运行时方法替换或 native 层替换。

### 3. Android 14+ 动态代码只读

Android 14（API 34）及以上，对动态加载 DEX/JAR/APK 文件要求只读，否则会出现：

```text
SecurityException: Writable dex file '...' is not allowed.
```

当前 `DexLoader` 在加载前执行：

```java
patchFile.setReadOnly();
```

补丁导入流程则在覆盖旧补丁前通过 `PatchRepository.ensureWritable()` 恢复文件可写，解决补丁曾经被加载为只读后无法覆盖的问题。

优化目录使用：

```text
app.getCodeCacheDir()/patch_dex
```

注意：放入 `code_cache` 并不等价于文件只读，真正的关键是加载前显式调用 `setReadOnly()`。

---

## 七、插件资源实现

### 1. 当前资源策略

插件使用 Android Gradle Plugin 正常编译：

- 插件资源默认使用 `packageId = 0x7f`；
- 不使用手写 Python `aapt2` 链路；
- 不使用 `--package-id 0x80`；
- 不使用 `--shared-lib`；
- 不调用 `R.onResourcesLoaded()`；
- 不使用 `addAssetPathAsSharedLibrary()`。

插件和宿主虽然都可能生成 `0x7f` 资源 ID，但二者不会直接合并到同一个 `Resources` 对象中，而是分别使用独立的 `AssetManager` 和 `Resources` 对象。

因此资源隔离依赖：

```text
独立 AssetManager
        +
独立 Resources
        +
插件专用 Theme
        +
插件 Context
```

而不是依赖人为修改 package ID。

### 2. `PluginResources.init()`

插件初始化时：

1. 创建新的 `AssetManager`；
2. 反射调用 `addAssetPath(patchApkPath)`；
3. 检查返回 cookie，返回 0 时认为资源加载失败；
4. 使用插件 AssetManager 创建独立 `Resources`；
5. 复用宿主 DisplayMetrics 和 Configuration；
6. 创建绑定插件 Resources 的 Theme；
7. 复制宿主 Theme 的系统样式属性；
8. 验证插件布局、背景 drawable、图标 drawable 是否可以解析。

伪代码如下：

```java
AssetManager assetManager = new AssetManager();
int cookie = assetManager.addAssetPath(patchApkPath);

Resources pluginResources = new Resources(
        assetManager,
        hostResources.getDisplayMetrics(),
        hostResources.getConfiguration());

Resources.Theme pluginTheme = pluginResources.newTheme();
pluginTheme.setTo(host.getTheme());
```

### 3. 为什么必须替换 Theme

只重写 `Context.getResources()` 和 `Context.getAssets()` 还不够。

XML inflate 创建 `LinearLayout`、`TextView`、`ImageView` 等 View 时，会通过：

```java
context.obtainStyledAttributes(...)
```

解析 `android:background`、`android:src`、`android:textColor` 等属性。

`obtainStyledAttributes()` 最终使用的是 `context.getTheme()`。如果仍使用宿主 Theme，宿主 Resources 就会尝试读取插件的资源 ID，例如：

```text
0x7f020000 = plugin_bg
```

宿主没有这个插件 drawable，就会出现：

```text
Resources$NotFoundException:
Unable to find resource ID #0x7f020000
```

当前通过 `PluginContext` 同时替换：

```text
getResources()
getAssets()
getTheme()
```

确保 View 属性解析也走插件 Resources。这是 XML 资源能够正常工作的关键修复。

### 4. 插件 XML inflate

`PluginResources.inflate()` 使用：

```java
LayoutInflater.from(host)
        .cloneInContext(pluginContext)
        .inflate(layoutId, null);
```

插件 Context 内部：

- `getResources()` 返回插件 Resources；
- `getAssets()` 返回插件 AssetManager；
- `getTheme()` 返回插件 Theme；
- `setTheme()` 作用于插件 Theme。

所以当前插件布局中的以下资源均可正常加载：

```xml
android:background="@drawable/plugin_bg"
android:src="@drawable/plugin_icon"
android:text="@string/plugin_title"
android:textColor="@color/plugin_primary"
```

---

## 八、插件 Activity 代理机制

### 1. 为什么不直接启动插件 Activity

插件 APK 中的页面类不直接继承 `android.app.Activity`，也不在宿主 Manifest 中声明。原因是插件页面需要：

- 使用插件自己的 Resources；
- 使用插件自己的 Theme；
- 与宿主 Activity 生命周期解耦；
- 避免把未知插件组件直接交给系统 Activity 启动流程。

当前采用宿主占位 Activity + 插件普通类的代理模式，机制对齐 360 线上项目的核心结构。

### 2. 启动流程

```text
PluginSdk.startPluginActivity(context)
        │
        ▼
启动 sdk 中的 HostProxyActivity
        │
        ▼
HostProxyActivity 创建 ApkPluggingActivityProxy
        │
        ▼
反射加载 plugin 中的 ApkProxyActivity
        │
        ▼
ApkProxyActivity 读取 plugin_view_id
        │
        ▼
创建 PluginActivity 普通 Java 对象
        │
        ▼
PluginActivity.onCreate(Activity, Bundle)
        │
        ▼
PluginBaseActivity.setContentView()
        │
        ▼
PluginResources.inflate(plugin_activity.xml)
        │
        ▼
插件 View 设置到宿主 HostProxyActivity
```

### 3. 类职责

#### `HostProxyActivity`

位于 SDK AAR 内，是宿主 Manifest 中唯一需要注册的插件承载 Activity。

它负责接收 Android Framework 生命周期，并转发：

- `onCreate`
- `onStart`
- `onRestart`
- `onActivityResult`
- `onResume`
- `onPause`
- `onStop`
- `onDestroy`
- `onSaveInstanceState`
- `onNewIntent`
- `onRestoreInstanceState`
- `onTouchEvent`
- `onKeyUp`
- `onWindowAttributesChanged`
- `onWindowFocusChanged`
- `onBackPressed`
- 菜单相关回调

#### `ApkPluggingActivityProxy`

位于 SDK AAR 内，通过反射加载：

```text
com.plugin.sdk.plugin.ApkProxyActivity
```

它不直接依赖插件 Java 类型，通过约定的方法签名进行生命周期转发。

#### `ApkProxyActivity`

位于插件 APK 内，是插件侧生命周期总代理，根据 `plugin_view_id` 创建具体插件页面。

当前：

```text
plugin_view_id = 1 → new PluginActivity()
```

#### `PluginBaseActivity`

位于插件 APK 内，但不是 Android Activity。它持有：

```java
protected Activity mActivity;
protected View mBaseView;
```

提供：

- `setContentView(Activity, int layoutId)`；
- `findViewById(int id)`；
- 插件页面与宿主 Activity 之间的桥接。

#### `PluginActivity`

当前示例插件页面：

```java
public class PluginActivity extends PluginBaseActivity
        implements ApkInterfaceForProxyActivity
```

它通过插件 Resources inflate `plugin_activity.xml`，然后把根 View 设置到宿主 `HostProxyActivity` 上。

---

## 九、插件入口和 View 加载

### 1. `PluginEntry`

宿主通过反射加载：

```text
com.plugin.sdk.plugin.PluginEntry
```

当前提供：

```java
public static String getVersion();
public static void initResources(Context context, String patchPath);
public static View createView(Context context);
```

### 2. 直接加载插件 View

Demo 的“加载补丁 View（dex+资源）”按钮会：

1. 反射加载 `PluginEntry`；
2. 反射调用 `createView(Context)`；
3. 插件通过 `PluginResources.inflate()` 加载 XML；
4. 返回 View；
5. Demo 将 View 放入自己的 `FrameLayout`。

这条路径验证的是：

```text
dex 加载 + PluginEntry 反射 + XML 资源 + drawable + color + string
```

### 3. 启动插件 Activity

Demo 的“启动补丁 Activity”按钮调用：

```java
PluginSdk.startPluginActivity(this);
```

这条路径验证的是：

```text
宿主占位 Activity + 生命周期代理 + 插件页面 + 插件 XML 资源
```

---

## 十、签名校验

### 1. 当前校验模型

当前采用：

```text
插件 APK 签名证书 == 宿主 APK 签名证书
```

如果补丁被修改，签名证书不一致，补丁不会进入 dex 加载阶段。

### 2. 当前 API 级别策略

- API 28 及以上：使用 `PackageManager.GET_SIGNING_CERTIFICATES`；
- 读取 `SigningInfo`；
- 使用 `getApkContentsSigners()` 获取当前 APK 签名证书；
- API 27 及以下：使用 `GET_SIGNATURES`，走 V1 证书比对；
- 当前日志会输出补丁和宿主证书 SHA-256 指纹。

### 3. 当前 demo 签名

当前骨架的 `plugin` release 使用本机 debug keystore，demo 也使用相同 debug 签名，因此可以通过验签：

```text
~/.android/debug.keystore
alias: androiddebugkey
password: android
```

生产环境必须替换为企业正式签名，并确保宿主和插件使用同一签名体系。

### 4. 关于 V2-only 要求

最初方案要求“只使用 V2 签名，并以 V2 为准”。当前骨架代码在 API 28+ 通过 `SigningInfo` 进行证书校验，并没有单独解析 APK Signing Block 来强制检查“仅 V2、禁止 V1/V3”。

因此当前实现的准确状态是：

- API 28+：基于系统 `SigningInfo` 校验证书；
- API 27 及以下：使用 V1 兼容校验；
- 当前 AGP/debug 签名不是严格意义上的 V2-only。

如果生产环境仍然必须执行严格的 V2-only 策略，需要后续增加 APK Signing Block 级别的 V2 校验，并在构建签名配置中明确关闭不需要的签名方案。当前 demo 骨架暂以“宿主和插件证书一致”为验收标准。

---

## 十一、自动化构建流程

### 1. 当前唯一推荐命令

从项目根目录执行：

```bash
./gradlew :demo:assembleDebug
```

Windows 下也可以执行：

```bat
gradlew.bat :demo:assembleDebug
```

### 2. 自动执行链

该命令内部自动完成：

```text
:demo:assembleDebug
        │
        ├─ demo/build.gradle 检测到 assembleDebug
        │
        ├─ 嵌套执行 :plugin:assembleRelease
        │
        ├─ plugin/build.gradle 的 assembleRelease.doLast
        │       自动复制：
        │       plugin/build/outputs/apk/release/plugin-release.apk
        │       → demo/src/main/assets/patch.apk
        │
        └─ 继续完成 demo:assembleDebug
```

这套机制参考：

```text
D:/AndroidCode/360_android_mobile_sdk/sdk_social_pay_demo_online/build.gradle
```

并保留了防递归判断：

- 仅处理 `assembleDebug`；
- 仅当根任务参数中包含 `:demo:assembleDebug` 时触发；
- Windows 使用根目录下 `gradlew.bat` 的绝对路径；
- 不会因为嵌套执行插件任务而再次递归触发 demo 构建。

### 3. 单独构建插件

如需单独验证插件 APK，可以执行：

```bash
./gradlew :plugin:assembleRelease
```

产物：

```text
plugin/build/outputs/apk/release/plugin-release.apk
```

该任务完成后同样会自动复制：

```text
demo/src/main/assets/patch.apk
```

### 4. 构建 SDK AAR

```bash
./gradlew :sdk:assembleDebug
```

产物：

```text
sdk/build/outputs/aar/sdk-debug.aar
```

### 5. 当前工具链

| 工具 | 版本 |
|---|---|
| Android Gradle Plugin | 9.4.0 |
| Gradle | 9.7.1 |
| compileSdk | 37 |
| targetSdk | 37 |
| minSdk | 21 |
| Java | 17 |

项目已经移除 AGP 9 不再支持的配置：

```text
android.defaults.buildfeatures.buildconfig
android.nonFinalResIds
```

同时不再手动指定过期的 `buildToolsVersion`。

---

## 十二、验收流程

### 方式 A：自动构建并内置补丁

```bash
./gradlew :demo:assembleDebug
adb install -r demo/build/outputs/apk/debug/demo-debug.apk
```

打开 App 后检查：

1. 状态显示补丁已加载；
2. 版本显示 `2.0.0`；
3. 点击“加载补丁 View（dex+资源）”；
4. 插件 XML、背景、图标、字符串、颜色正常显示；
5. 点击“启动补丁 Activity”；
6. 插件代理 Activity 正常启动；
7. 返回、暂停、恢复等生命周期可以正常转发。

当前上述 View 和 Activity 验收均已在真机通过。

### 方式 B：运行时导入补丁

```bash
./gradlew :plugin:assembleRelease
adb push plugin/build/outputs/apk/release/plugin-release.apk /sdcard/Download/patch.apk
```

然后：

1. 启动 demo；
2. 点击“从 sdcard 导入补丁”；
3. 关闭并重新启动 App；
4. 检查补丁状态和版本；
5. 分别验证 View 和 Activity。

### 常用日志

SDK 日志使用 `AppLogUtils`，tag 自动带有 `PluginSdk_` 前缀：

```bash
adb logcat -s PluginSdk_HotUpdateEngine:V PluginSdk_SignatureVerifier:V PluginSdk_PluginSdk:V
```

插件资源上下文使用 Android `Log` 输出：

```text
PluginResources
PluginBaseActivity
```

重点日志包括：

```text
开始加载补丁
验签结果
 dex 加载成功
补丁资源初始化成功
addAssetPath 成功
插件资源验证
inflate layout
```

---

## 十三、已经解决的问题

### 1. Android 14+ Writable dex 错误

错误：

```text
SecurityException: Writable dex file ... is not allowed
```

修复：加载前对补丁 APK 执行 `setReadOnly()`，导入新补丁前恢复可写。

### 2. 直接启动插件 Activity 的资源问题

原方案尝试直接把插件类作为真实 Activity 启动，存在宿主资源和插件资源上下文不一致问题。

修复：使用 `HostProxyActivity + ApkPluggingActivityProxy + ApkProxyActivity + PluginBaseActivity` 的代理结构。

### 3. 手工 aapt2 / packageId 方案不稳定

曾经尝试过：

```text
aapt2 --package-id 0x80
aapt2 --shared-lib
addAssetPathAsSharedLibrary()
R.onResourcesLoaded()
Python build_patch.py
```

实际出现过：

```text
No package ID 7f found
Unable to find resource ID
Resources$NotFoundException
```

最终改为 Android Gradle Plugin 正常编译插件 APK，并使用独立 `Resources` 对象隔离资源。

### 4. XML inflate 时 `0x7f020000` 找不到

错误：

```text
Unable to find resource ID #0x7f020000
```

根因不是资源没有打入 APK，而是插件 Context 只替换了 `getResources()`，没有替换 `getTheme()`。View 构造时仍通过宿主 Theme 解析插件资源 ID。

修复：

- 创建插件独立 Theme；
- `PluginContext` 重写 `getTheme()`；
- XML 属性解析改用插件 Resources；
- `plugin_bg`、`plugin_icon`、布局资源均已验证通过。

### 5. 插件页面异常被吞掉

`PluginBaseActivity` 原先吞掉 `setContentView()` 异常，导致排查困难。

当前已改为输出完整异常日志。

---

## 十四、当前限制和生产化注意事项

### 1. 类替换限制

当前方案不是全量运行时方法替换：

- 已加载类不保证可以替换；
- 需要重启 App；
- 不支持修改已经执行中的 ART 方法；
- 不支持 native 方法热替换。

### 2. 插件依赖限制

当前最小插件只依赖 Android Framework API，没有依赖 AndroidX 或其他三方库。

如果插件后续使用 AndroidX 或其他库，必须在插件构建工程中明确管理依赖，并考虑：

- 插件库是否需要随插件 APK 一起打包；
- 宿主是否已经存在相同依赖；
- 类冲突和版本冲突；
- 插件与宿主的 ClassLoader 优先级。

### 3. 反射和隐藏 API 风险

当前方案使用了：

- `BaseDexClassLoader.pathList`；
- `DexPathList.dexElements`；
- `AssetManager.addAssetPath`。

这些接口属于 Android 内部实现或隐藏 API。当前目标是企业内部分发，已在目标环境验证；若后续适配更多 Android 版本，需要持续做兼容性验证。

### 4. 当前补丁来源

骨架当前支持：

- `assets/patch.apk`；
- `/sdcard/Download/patch.apk`。

生产环境不建议直接信任任意外部文件，应该由 SDK 内部统一管理下载和导入流程。

### 5. 当前缺少的生产能力

当前骨架还没有完整实现：

- HTTPS 下载；
- 下载临时文件；
- 显式 SHA-256 文件摘要校验；
- 版本号比较；
- 灰度发布；
- 回滚和多版本保留；
- 断点续传；
- 补丁过期策略；
- 下载失败重试。

---

## 十五、生产可信加载链

生产环境建议使用以下顺序：

```text
1. 从 HTTPS 下载到临时文件
2. 下载完成并关闭文件
3. 校验文件长度和 SHA-256
4. 校验插件 APK 签名证书
5. 校验版本号和兼容范围
6. rename 为正式补丁文件
7. setReadOnly()
8. 记录当前补丁元数据
9. 重启 App
10. DexClassLoader 加载
```

推荐目录结构：

```text
filesDir/
└── plugins/
    └── <plugin-name>/
        ├── current.apk
        ├── current.json
        └── backup/
            ├── 1.0.0.apk
            └── 1.1.0.apk
```

元数据示例：

```json
{
  "plugin": "main",
  "version": "2.0.0",
  "sha256": "...",
  "certificateSha256": "...",
  "minHostVersion": "1.0.0"
}
```

补丁必须在校验完成、重命名成功后才进入正式路径，避免加载到未下载完整或被替换中的 APK。

---

## 十六、后续扩展方向

1. 将 `plugin_view_id` 扩展为插件页面注册表；
2. 增加插件版本比较和回滚；
3. 增加 HTTPS 下载和 SHA-256 校验；
4. 增加补丁下载临时目录与原子替换；
5. 增加多插件管理；
6. 增加插件能力白名单；
7. 增加插件生命周期异常上报；
8. 增加 release 签名和严格 V2 校验；
9. 增加自动化真机回归测试；
10. 根据目标 Android 版本持续验证 dexElements 和 AssetManager 反射兼容性。

---

## 十七、最终结论

当前 PluginSdk 已经形成可运行的三层架构：

```text
第三方宿主 App
        │
        ├── 接入 sdk AAR
        ├── 注册 HostProxyActivity
        └── Application 调用 PluginSdk.init()
                │
                ▼
        SDK 热更新运行时
                │
                ├── 验签
                ├── DexClassLoader 加载 dex
                ├── 合并 dexElements
                ├── 初始化插件独立 Resources
                └── 代理插件 Activity 生命周期
                        │
                        ▼
                plugin APK
                ├── 插件 Java 代码
                ├── XML 布局
                ├── drawable
                ├── color
                └── string
```

当前已经验证：

- 插件 APK 可以由 Gradle 正常编译；
- demo 构建可以自动触发插件编译和复制；
- APK 签名校验通过；
- Android 14+ 动态 dex 只读要求已处理；
- 插件 dex 可以加载；
- 插件独立 XML 资源可以 inflate；
- 插件 View 可以正常加载；
- 插件代理 Activity 可以正常启动；
- 插件 Activity 生命周期可以转发；
- 无需第三方插件化框架；
- 接入方只需要接入一次 SDK AAR。

当前推荐构建命令：

```bash
./gradlew :demo:assembleDebug
```

该命令会自动完成：

```text
编译 plugin → 复制 patch.apk → 编译 demo
```
