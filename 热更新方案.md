# PluginSdk 插件化热更新方案（当前实现）

> 当前状态：最小可运行方案已完成并在真机验证通过。插件 View 可以正常加载，插件代理 Activity 可以正常启动，插件 XML、字符串、颜色和 drawable 资源可以正常渲染。

## 一、方案结论

- 对外提供 `sdk` 模块生成的 AAR。
- 第三方 App 只接入一次 SDK，后续通过插件 APK 更新代码和 UI。
- 插件由 Android Gradle Plugin 编译成独立 APK。
- 插件支持 dex、XML 布局、字符串、颜色和 drawable 资源。
- 更新后的插件需要重启 App 生效。
- 只保证新增类和"尚未被加载类"的替换，不做已加载类的方法级替换。
- 目标场景：minSdk 21、ART、国内 ToB / 企业内部分发。

## 二、三层结构

```text
PluginSdk/
├── sdk/       Android library，对外输出 AAR
├── demo/      模拟第三方接入方的测试 App
└── plugin/    可热更插件，输出插件 APK
```

### `sdk`

SDK 运行时，负责：

- `PluginSdk` 对外 API；
- 插件定位、导入和状态管理；
- APK 签名校验；
- dex 加载和类优先级处理；
- 反射触发插件侧的插件资源初始化；
- `HostProxyActivity` 宿主占位 Activity；
- 插件 Activity 生命周期代理；
- `EdgeToEdge` 全屏适配；
- 日志和错误信息。

### `demo`

模拟真实接入方：

- 依赖 `sdk`；
- 在 `Application.onCreate()` 调用 `PluginSdk.init(this)`；
- 注册 `HostProxyActivity`；
- 界面来自宿主自己的 XML 布局 `res/layout/activity_main.xml`；
- 验证插件 View 和插件 Activity。

真实接入方不需要依赖 `plugin` 模块。

宿主拥有自己的 `0x7f` 资源，与插件资源 ID 完全撞车（例如双方都有
`0x7f020000`），但由于二者分属不同的 `Resources` 对象，各自仍能正确解析，
这也是资源隔离效果最直接的验证场景。

### `plugin`

独立 Android application 模块，由 AGP 编译为完整插件 APK，包含：

- `classes.dex`；
- `resources.arsc`；
- XML、drawable、颜色、字符串等资源；
- 插件入口和页面代码。

插件中的页面不是 `android.app.Activity`，而是普通 Java 类，由 SDK 的宿主占位 Activity 承载。

插件独立 `Resources` / `Theme` 的实现也在这个模块（`PluginResources`）：宿主只负责
反射调用 `PluginEntry.initResources()`，真正的资源隔离逻辑在插件侧，可以随插件热更。

插件模块不依赖 `sdk`，只使用 `android.jar`，因此插件工程可以独立编译。插件侧日志统一用
`android.util.Log`，不使用 SDK 的 `AppLogUtils`。

## 三、接入方接入方式

### 1. 初始化

```java
public class MyApplication extends Application {
    @Override
    public void onCreate() {
        super.onCreate();
        PluginSdk.init(this);
    }
}
```

### 2. 注册占位 Activity

接入方只需要注册 SDK 提供的占位组件：

```xml
<activity
    android:name="com.plugin.sdk.activity.HostProxyActivity"
    android:exported="false"
    android:theme="@android:style/Theme.Material.NoActionBar" />
```

插件页面本身不是 `android.app.Activity`，不需要在宿主 Manifest 中声明。

### 3. 对外 API

| API | 作用 |
|---|---|
| `PluginSdk.init(Application)` | 初始化热更新引擎（异步，无回调） |
| `PluginSdk.init(Application, LoadCallback)` | 初始化热更新引擎（异步，加载完成后主线程回调） |
| `PluginSdk.isPluginLoaded()` | 查询插件是否加载成功 |
| `PluginSdk.getPluginVersion()` | 获取插件版本，读不到时返回 null |
| `PluginSdk.getPluginPath()` | 获取当前插件路径 |
| `PluginSdk.getLastError()` | 获取最近一次失败原因，无错误时为 null |
| `PluginSdk.importPlugin(Context, File)` | 从指定文件导入插件（推荐，不受分区存储限制） |
| `PluginSdk.importPlugin(Context)` | 从 `/sdcard/Download/plugin_main.apk` 导入插件（见下方限制） |
| `PluginSdk.startPluginActivity(Context)` | 启动默认插件页面（`DEFAULT_PLUGIN_VIEW_ID`） |
| `PluginSdk.startPluginActivity(Context, int viewId)` | 启动指定插件页面 |
| `PluginSdk.DEFAULT_PLUGIN_VIEW_ID` | 默认插件页面标识常量，值为 1 |
| `AppLogUtils.setEnabled(boolean)` | 运行期开关日志输出 |

`LoadCallback` 接口定义：

```java
public interface LoadCallback {
    void onLoadFinished(boolean success, String version, String error);
}
```

`init` 是**异步**的：调用后立即返回，插件在后台线程加载，加载完成后在主线程回调
`LoadCallback`。未传回调时可改用 `isPluginLoaded()` 查询状态。因此接入方如果在
`init` 后立即调用 `startPluginActivity()`，可能因插件尚未加载完成而抛
`IllegalStateException`，需通过回调或查询确认就绪。

关于 `importPlugin(Context)`：目标文件路径是 `/sdcard/Download/plugin_main.apk`，
但 targetSdk 30 起应用无权按文件路径读取 Download 目录下的非媒体文件，
该方法在 Android 11+ 上会失败。真机验证请改用 `importPlugin(Context, File)`，
或由接入方把插件下载到自己的 filesDir / cacheDir 后再导入。

`viewId` 对应的插件页面由插件侧的 `ApkProxyActivity` 注册表决定，每次调用都会启动
一个新的 `HostProxyActivity` 实例，页面栈由 Activity 返回栈承载。

## 四、Edge-to-edge 全屏适配

SDK 内置 `com.plugin.sdk.utils.EdgeToEdge`，用 framework API 实现全屏适配，SDK 不引入 AndroidX。

### 1. 接入方在自己的 Activity 上调用（可选）

SDK 提供该 API 供接入方给自己的页面做全屏适配，demo 本身没有调用它，
插件页面的全屏是 `HostProxyActivity` 内部开启的。

```java
@Override
protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    EdgeToEdge.enable(this);
    ...
}
```

### 2. 插件页面自动开启

`HostProxyActivity` 在 `onCreate` 中固定调用 `EdgeToEdge.enable(this)`，插件页面开箱即全屏，
不提供开关。

### 3. 内部实现

按 API 级别分支，覆盖 minSdk 21 起全部版本：

| 能力 | framework API | 引入版本 |
|---|---|---|
| 内容延伸到系统栏下 | `window.setDecorFitsSystemWindows(false)` | API 30 |
| 同上，低版本回退 | `decorView.setSystemUiVisibility(LAYOUT_STABLE\|LAYOUT_FULLSCREEN\|LAYOUT_HIDE_NAVIGATION)` | API 16 |
| 状态栏颜色 | `window.setStatusBarColor(int)` | API 21 |
| 导航栏颜色 | `window.setNavigationBarColor(int)` | API 21 |
| 强制对比度蒙层 | `window.setStatusBarContrastEnforced` / `setNavigationBarContrastEnforced` | API 29 |
| 状态栏亮色图标 | `SYSTEM_UI_FLAG_LIGHT_STATUS_BAR`；API 30+ 用 `getInsetsController()` | API 23 |
| 导航栏亮色图标 | `SYSTEM_UI_FLAG_LIGHT_NAVIGATION_BAR`；API 30+ 同上 | API 26 |

API 30 才存在的 `WindowInsetsController` 等类型集中在内部类 `EdgeToEdge$Api30` 中，
只在 API 30+ 被加载，低版本设备不会对该类做校验。

### 4. 注意事项

1. **API 35+ 蒙层颜色失效**：targetSdk 35 起系统强制 edge-to-edge，
   `setStatusBarColor` / `setNavigationBarColor` 变为 no-op，`SystemBarStyle` 传入的
   scrim 颜色在 Android 15+ 上不会生效。这是平台行为，无法绕过。
2. **插件页面需要处理 inset 避让**：开启全屏后内容会绘制到系统栏下方，
   插件布局需要自行留出状态栏/导航栏间距，否则内容可能被遮挡。
3. **接入方自定义样式**：可用 `EdgeToEdge.enable(activity, statusBarStyle, navigationBarStyle)`
   分别定制状态栏和导航栏，`SystemBarStyle` 提供 `auto` / `dark` / `light` 三种预设，
   并支持通过 `DarkModeDetector` 接入自己的夜间模式判断。

## 五、插件启动流程

`PluginSdk.init()` 内部在后台线程（`PluginSdk-Loader`）执行 `load()`，立即返回、不阻塞
主线程。加载流程如下：

```text
Application.onCreate()
        ↓
PluginSdk.init()
        ↓
HotUpdateEngine.load()
        ├─ 扫描 filesDir/plugin/，按 versionCode 从高到低排序
        ├─ 无插件时尝试导入 assets/plugin_main.apk
        └─ 对每个版本依次尝试（降级）：
            ├─ 三码校验：文件名 versionCode == APK 元数据 versionCode == dex 常量 versionCode
            ├─ 校验插件签名
            ├─ DexLoader 加载 classes.dex
            └─ 初始化插件 Resources
               （任一步失败 → 降级尝试次高版本；全部失败则插件未加载）
```

### 版本目录

插件按版本号平铺存放，导入新版本**永不删除旧版本**，每个旧版本就是一个可回退点：

```text
/data/user/0/<host-package>/files/plugin/
├── plugin_main_200.apk     ← versionCode=200（最新，优先加载）
├── plugin_main_100.apk     ← versionCode=100（次新，降级候选）
└── plugin_main.tmp         ← 导入临时文件
```

`assets/plugin_main.apk` 仅用于 demo 快速验收（不带版本号，导入时按包内 versionCode 命名）。

### 版本号单一真源

插件版本号只在 `PluginEntry` 定义，`plugin/build.gradle` 用 Groovy 函数从这里解析出
`versionCode` / `versionName`，保证 APK 元数据版本与 dex 内常量版本天然一致：

```java
// PluginEntry.java
public static final String PLUGIN_VERSION_NAME = "1.0.0";
public static final int PLUGIN_VERSION_CODE = 100;
```

改插件版本只需改这两个常量，APK 元数据版本自动跟随。

### 三码校验

加载前校验「文件名 versionCode == APK 元数据 versionCode」（都不需要 dex），
加载后校验「dex 常量 versionCode == 文件名 versionCode」（反射 `PluginEntry.getVersionCode()`）。
任一不一致即视为被篡改或串版本，拒绝加载并降级。

## 六、dex 热更新

`DexLoader` 的核心流程：

1. 对插件 APK 执行 `setReadOnly()`；
2. 使用 `DexClassLoader` 加载插件；
3. 反射读取宿主和插件的 `BaseDexClassLoader.pathList.dexElements`；
4. 将插件 dexElements 放到宿主 dexElements 前面；
5. 写回宿主 ClassLoader。

```text
merged dexElements = pluginElements + hostElements
```

因此后续类加载会优先命中插件类。

### 生效边界

- 新增类可以加载；
- 尚未被 ART 加载的类可以由插件优先提供；
- 已加载类不保证可以替换；
- 导入插件后需要重启 App；
- 不涉及 ArtMethod 或 native 方法热替换。

### Android 14+ 只读要求

targetSdk 34 及以上时，在 Android 14+ 设备上动态加载的 DEX/JAR/APK 必须只读，否则会出现：

```text
SecurityException: Writable dex file ... is not allowed
```

当前加载前显式执行：

```java
pluginFile.setReadOnly();
```

插件重新导入前，`PluginRepository` 会先恢复旧文件可写，避免覆盖失败。优化目录使用：

```text
app.getCodeCacheDir()/plugin_dex
```

放入 `code_cache` 本身不等于只读，`setReadOnly()` 才是关键。

## 七、插件资源方案

### 1. 资源隔离方式

插件使用 AGP 正常编译，资源默认使用 `0x7f`。插件与宿主都可能拥有 `0x7f` 资源 ID，
但两者使用不同的 `AssetManager` 和 `Resources` 对象，因此不会在同一个资源表中冲突。

当前 demo 与插件就处于完全撞车的状态：

| 资源 ID | 宿主（demo） | 插件（plugin） |
|---|---|---|
| `0x7f010000` | `id/btn_import_plugin` | `color/plugin_primary` |
| `0x7f020000` | `layout/activity_main` | `drawable/plugin_bg` |
| `0x7f030000` | `string/btn_import_plugin` | `id/iv_plugin_logo` |

双方同为 `0x7f` 且 ID 逐一冲突，仍能各自解析到自己的资源。

### 2. 初始化资源

`PluginResources.init()` 会：

1. 创建独立 `AssetManager`；
2. 通过 `addAssetPath(pluginApkPath)` 加载插件 APK；
3. 检查返回 cookie，为 0 视为加载失败；
4. 创建只包含插件资源的 `Resources`；
5. 复用宿主的 DisplayMetrics 和 Configuration；
6. 验证插件布局、背景和图标资源。

Theme 不在这里创建，见下一节。

### 3. 插件 Context 和 Theme

XML inflate 创建 View 时，除了 `getResources()`，还会通过 `context.obtainStyledAttributes()`
使用 `context.getTheme()` 解析 `background`、`src`、`textColor` 等属性。

如果只替换 `getResources()` 而沿用宿主的 Theme，View 属性解析会去宿主的资源表里查插件的
资源 ID，导致：

```text
Resources$NotFoundException: Unable to find resource ID #0x7f020000
```

`PluginResources.inflate()` 每次都会基于当前承载它的 Context 新建一个 `PluginContext`
（继承 `ContextThemeWrapper` 的私有内部类，对齐 360 的 PluginContext）。重写
`getResources()` / `getAssets()`；Theme 交给 `ContextThemeWrapper` 懒加载——首次
`getTheme()` 时会用「插件 Resources」newTheme 并 setTo(host 的 theme)，同时
`getSystemService(LAYOUT_INFLATER_SERVICE)` 会自动 cloneInContext 锚定整棵 View 树。

inflate 时使用：

```java
LayoutInflater.from(host)
        .cloneInContext(pluginContext)
        .inflate(layoutId, null);
```

两个实现细节：

- **Theme 按次构造，不缓存**。`init()` 拿到的是 Application，只能取到 application 的
  theme，而宿主真正生效的主题通常声明在 Activity 上。因此每次 inflate 都用传入的
  `host.getTheme()` 新建 Theme，保证插件控件样式跟随当前页面。
- **inflate 前刷新一次 Configuration**。独立 `Resources` 不在 ResourcesManager 的托管
  列表里，框架不会在旋转、深色模式、语言切换时自动更新它，需要显式
  `updateConfiguration()`。

注意 `Theme.setTo()` 跨 `Resources` 使用时只复制双方共有的属性，也就是 framework 段
（`0x01xxxxxx`）的属性，宿主 App 自定义的主题属性不会带过来。

### 4. 资源取用入口

插件页面在代码里动态取资源时，必须走 `PluginResources` 的便捷方法，而不是宿主
Context 的 `getResources().getDrawable()`（那会拿到宿主的资源表，把 0x7f 隔离打穿）：

```java
PluginResources.getDrawable(R.drawable.xxx)   // 动态取 drawable
PluginResources.getString(R.string.xxx)        // 动态取字符串
PluginResources.getColor(R.color.xxx)          // 动态取颜色
PluginResources.getResources()                  // 拿插件 Resources 本体
```

## 八、插件 Activity 代理

插件 Activity 采用宿主占位 Activity + 普通 Java 类的代理模式：

```text
PluginSdk.startPluginActivity()
        ↓
启动 sdk 的 HostProxyActivity
        ↓
ApkPluggingActivityProxy 反射加载插件 ApkProxyActivity
        ↓
ApkProxyActivity 根据 plugin_view_id 创建 PluginActivity
        ↓
PluginActivity 通过 PluginBaseActivity 加载插件 XML
        ↓
插件 View 设置到 HostProxyActivity
```

主要类职责：

| 类 | 位置 | 职责 |
|---|---|---|
| `HostProxyActivity` | sdk | 宿主真正注册和启动的 Activity |
| `ApkPluggingActivityProxy` | sdk | 宿主侧反射代理，转发生命周期 |
| `ApkProxyActivity` | plugin | 插件侧生命周期总代理，按 viewId 分发页面 |
| `PluginBaseActivity` | plugin | 页面基类，持有宿主 Activity 和插件根 View |
| `PluginActivity` | plugin | 示例插件页面 |

### 1. 生命周期接口

`ApkInterfaceForProxyActivity` 在 `sdk`（`com.plugin.sdk.activity`）和 `plugin`
（`com.plugin.sdk.plugin`）中各有一份，共 19 个方法。两份之间通过反射按
「方法名 + 参数签名」对接，不共享类型，因此插件可以完全不依赖 SDK 独立编译。

- sdk 那份约束 `ApkPluggingActivityProxy` 实现全部 19 个方法；
- plugin 那份约束插件页面实现全部 19 个方法。

### 2. 插件页面写法

`PluginBaseActivity` 已实现该接口并给出全部空默认实现，插件页面只需要重写关心的方法：

```java
public class PluginActivity extends PluginBaseActivity {

    @Override
    public void onCreate(Activity activity, Bundle savedInstanceState) {
        setContentView(activity, R.layout.plugin_activity);
        // ...
    }

    @Override
    public void onBackPressed() {
        Activity activity = getActivity();
        if (activity != null) {
            activity.finish();
        }
    }
}
```

新增插件页面时，可以在 `ApkProxyActivity` 里按 `plugin_view_id` 增加分发分支。

### 3. 异常处理

`ApkPluggingActivityProxy` 的每个转发方法都会捕获异常并记录日志，不会静默吞掉。
插件页面里抛出的异常会被拆掉 `InvocationTargetException` 包装层后打印，日志首行
直接指向插件侧的真实崩溃点。

## 九、插件入口和 View 加载

宿主通过反射调用：

```text
com.plugin.sdk.plugin.PluginEntry
```

当前入口方法：

```java
public static String getVersion();
public static void initResources(Context context, String pluginPath);
public static View createView(Context context);
```

直接加载 View 的流程：

```text
反射 PluginEntry.createView()
        ↓
PluginResources.inflate()
        ↓
加载插件 XML、drawable、color、string
        ↓
返回 View 并嵌入宿主页面
```

这条路径用于把插件 View 嵌入宿主已有页面，与第八节的插件 Activity 路径并存。

## 十、签名校验

当前安全模型：

```text
插件 APK 签名证书 == 宿主 APK 签名证书
```

实现策略：

- API 28+：`GET_SIGNING_CERTIFICATES` + `SigningInfo` + `getApkContentsSigners()`；
- API 27 及以下：`GET_SIGNATURES`，走 V1 证书比对；
- 日志输出插件和宿主证书 SHA-256 指纹；
- 验签通过后才会进入 dex 加载阶段。

### 当前签名配置

| 模块 | 构建类型 | 签名来源 |
|---|---|---|
| plugin | release | `~/.android/debug.keystore`（alias `androiddebugkey`） |
| demo | debug | AGP 默认 debug keystore，即同一个 `~/.android/debug.keystore` |

所以当前能通过验签的组合是 **demo 的 debug 包 + plugin 的 release 包**。

demo 的 release 构建**没有配置签名**，产出的是未签名 APK；如果要用 release 验证，
必须先给 demo 配好与 plugin 一致的签名，否则必然报"插件签名校验失败"。

生产环境必须替换为企业正式签名，并保证宿主和插件使用同一签名证书。

## 十一、自动化构建

### 推荐命令

从项目根目录执行：

```bash
./gradlew :demo:assembleDebug
```

Windows 也可以执行：

```bat
gradlew.bat :demo:assembleDebug
```

自动流程：

```text
:demo:assembleDebug
        ↓
执行 :plugin:assembleRelease（嵌套 Gradle 调用）
        ↓
复制 plugin-release.apk
        ↓
demo/src/main/assets/plugin_main.apk
        ↓
继续编译 demo
```

因此不需要手动执行插件编译和 `cp`。

嵌套构建挂在 demo `preBuild` 任务的 `doFirst` 上，属于执行阶段。放在配置阶段会早于
`clean` 执行，导致 `gradlew clean :demo:assembleDebug` 里刚构建出的插件 APK 又被 `clean` 删掉。

### 触发范围

| 命令 | 是否触发插件构建 |
|---|---|
| `:demo:assembleDebug` | ✅ |
| `clean :demo:assembleDebug` | ✅ |
| `assembleDebug`（根目录，不带模块前缀） | ✅ |
| `:plugin:assembleRelease` | ✅（就是插件自己的构建） |
| `:sdk:assembleDebug` | ❌ |
| `help` | ❌ |
| `:demo:assembleRelease` | ❌（见下方说明） |

说明：`gradle.startParameter.taskNames` 是用户原样输入、不做归一化的字符串列表，因此带模块
前缀和不带前缀两种写法都要显式匹配。`installDebug` 已覆盖（否则开发者用 `installDebug` 时
会静默打进上一次的旧插件），`assembleRelease` / `bundleRelease` 未覆盖——demo 的 release
构建没有配置签名，本来也无法通过验签，因此不纳入。

### 产物说明

`demo/src/main/assets/plugin_main.apk` 由插件构建自动生成并覆盖，已加入 `.gitignore`，不入库
（每次构建的签名字节都不同，入库只会产生无意义的二进制 diff）。

### 单独构建插件

```bash
./gradlew :plugin:assembleRelease
```

产物：

```text
plugin/build/outputs/apk/release/plugin-release.apk
```

插件构建完成后也会自动复制为 `demo/src/main/assets/plugin_main.apk`。

### 构建 SDK AAR

```bash
./gradlew :sdk:assembleDebug
```

产物：

```text
sdk/build/outputs/aar/sdk-debug.aar
```

### 当前工具链

```text
Android Gradle Plugin 9.4.0
Gradle 9.7.1
compileSdk 37
目标 SDK 37（demo / plugin；sdk 是 library 模块，不配置 targetSdk）
minSdk 21
Java 17
```

## 十二、验收

```bash
./gradlew :demo:assembleDebug
adb install -r demo/build/outputs/apk/debug/demo-debug.apk
```

启动 demo 后验证：

1. 状态显示插件已加载，版本为 `1.0.0`；
2. 点击"加载插件 View（dex+资源）"，插件布局和资源正常显示；
3. 点击"启动插件 Activity"，插件代理 Activity 正常启动；
4. 返回、暂停、恢复等生命周期正常转发。

运行时导入插件：

```bash
./gradlew :plugin:assembleRelease
adb push plugin/build/outputs/apk/release/plugin-release.apk /sdcard/Download/plugin_main.apk
```

上面这条 `adb push` 只在 Android 10 及以下可用。Android 11+ 应用无权按文件路径读取
Download 目录下的非媒体文件（targetSdk 30 起的分区存储限制），`importPlugin(Context)`
会抛 IOException。推荐由接入方把插件下载到自己的 filesDir / cacheDir 后调用
`importPlugin(Context, File)`——导入时会读包内 versionCode，自动命名成
`plugin_main_<versionCode>.apk`。

插件就位后重启 App 生效。导入的新版本不会删除旧版本（旧版本保留用于加载失败时降级）。

## 十三、日志和排查

SDK 使用 `AppLogUtils`，日志 tag 自动带 `PluginSdk_` 前缀，并支持超过 4K 自动分段：

```bash
adb logcat -s PluginSdk_HotUpdateEngine:V PluginSdk_SignatureVerifier:V PluginSdk_ApkPluggingActivityProxy:V
```

插件侧使用 `android.util.Log`（插件不引用 SDK 的类），关注：

```text
PluginResources
PluginBaseActivity
ApkProxyActivity
```

关键日志：

```text
开始加载插件
验签结果
 dex 加载成功
插件资源初始化成功
addAssetPath 成功
插件资源验证
inflate layout
插件 onCreate 调用失败
```

## 十四、当前限制和生产注意事项

1. 只保证新增类或未加载类的替换，插件导入后必须重启。
2. `DexClassLoader.pathList.dexElements`、`DexPathList.dexElements` 和 `AssetManager.addAssetPath` 属于内部/隐藏实现，需要持续验证 Android 版本兼容性。
3. 插件源码不依赖任何三方库，也没有引入 AndroidX；但 AGP 9 内置的 Kotlin 支持会自动把 `kotlin-stdlib` 打进插件 APK（插件 `classes.dex` 约 2 MB，绝大部分是 kotlin 标准库），如需控制插件体积需显式排除。插件侧日志使用 `android.util.Log`，不使用 SDK 的 `AppLogUtils`。
4. 插件来源支持 assets 与运行时导入。运行时导入受分区存储限制，Android 11+ 上 `/sdcard/Download` 路径不可用，应改用 `importPlugin(Context, File)`。已实现版本目录（多版本平铺、永不删旧版本）、三码校验和加载失败降级；尚未实现 HTTPS 下载、灰度、重试。
5. 插件的生效与回滚都是"重启级"的：`PluginRepository.clear()` 只删除插件文件，本次运行已经合并进宿主 ClassLoader 的插件 dex 无法撤销，重启后才回到无插件状态；同样地，导入新插件也不会影响本次运行已经加载的旧插件。
6. 生产下载流程应采用：

```text
下载到临时文件
→ SHA-256 校验
→ 签名校验
→ 版本校验
→ 原子 rename
→ setReadOnly()
→ 重启后加载
```

## 十五、当前已验证结论

- `sdk` 可以作为 AAR 对外提供；
- `demo` 可以自动触发插件编译；
- 宿主使用自己的 XML 布局和 `0x7f` 资源；
- 插件 APK 可以正常加载 dex；
- 宿主和插件签名校验通过；
- Android 14+ 动态 dex 只读问题已解决；
- 插件独立 XML Resources 和 Theme 已打通；
- 宿主与插件同为 `0x7f` 且 ID 冲突时仍可各自正确解析；
- 插件 View 可以正常加载；
- 插件代理 Activity 可以正常启动；
- 插件生命周期可以转发。

推荐构建命令：

```bash
./gradlew :demo:assembleDebug
```
